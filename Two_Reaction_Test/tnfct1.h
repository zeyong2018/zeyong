/* --------------------- DEKLARATIONEN tnfct1.h --------------------- */


/*--------------------------------------------------------------------*/
/* Prototypes for Test Functions                                      */
/*--------------------------------------------------------------------*/

#ifndef TNFCT1_H_INCLUDED

#define TNFCT1_H_INCLUDED

int fx1  (int n, REAL x[], REAL f[]);
int dfx1 (int n, REAL x[], REAL *jmat[]);

int fx2  (int n, REAL x[], REAL f[]);
int dfx2 (int n, REAL x[], REAL *jmat[]);

int fx3  (int n, REAL x[], REAL f[]);
int dfx3 (int n, REAL x[], REAL *jmat[]);

int fx4  (int n, REAL x[], REAL f[]);
int dfx4 (int n, REAL x[], REAL *jmat[]);

int fx5  (int n, REAL x[], REAL f[]);
int dfx5 (int n, REAL x[], REAL *jmat[]);

int fx6  (int n, REAL x[], REAL f[]);
int dfx6 (int n, REAL x[], REAL *jmat[]);

int fx7  (int n, REAL x[], REAL f[]);
int dfx7 (int n, REAL x[], REAL *jmat[]);

int fx8  (int n, REAL x[], REAL f[]);
int dfx8 (int n, REAL x[], REAL *jmat[]);

int fx9  (int n, REAL x[], REAL f[]);
int dfx9 (int n, REAL x[], REAL *jmat[]);

#endif

/* -------------------------- ENDE tnfct1.h ------------------------- */
